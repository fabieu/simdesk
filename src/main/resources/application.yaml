spring:
  application:
    name: acc-leaderboard
  jackson:
    default-property-inclusion: NON_NULL
    deserialization:
      fail-on-unknown-properties: false
  flyway:
    create-schemas: true
    schemas: acc_leaderboard
    default-schema: acc_leaderboard
management:
  endpoints:
    web:
      exposure:
        include: "info,health,metrics"
  info:
    java:
      enabled: true
logging:
  level:
    de.sustineo.acc.leaderboard: DEBUG
leaderboard:
  results:
    folder: "src/main/resources/examples"

---
spring:
  config:
    activate:
      on-profile: "production"
  datasource:
    url: "jdbc:mysql://localhost:3306/acc_leaderboard"
    username: "root"
    password: "my-secret-pw"
logging:
  level:
    de.sustineo.acc.leaderboard: INFO